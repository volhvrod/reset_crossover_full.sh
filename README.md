# 🚀 CrossOver Reset Script (macOS)

Универсальный Bash-скрипт для macOS, предназначенный для **полного сброса меток времени** как самого приложения CrossOver, так и указанной бутылки (Bottle). Это необходимо для обхода кэширования старых дат и обеспечения "свежего" запуска после истечения триального периода или установки обновлений.

Скрипт использует системные утилиты `osascript`, `pkill`, `defaults`, `plutil` и `sed` для глубокой очистки.

## 🛠️ Основные функции

  * **Принудительное закрытие CrossOver:** Гарантированное завершение всех процессов CrossOver для предотвращения блокировки файлов настроек.
  * **Сброс триала приложения:** Удаление ключей `FirstRunDate` и `SULastCheckTime` из системного файла настроек `com.codeweavers.CrossOver.plist`.
  * **Сброс метки бутылки:** Удаление внутренних файлов `.version` и `.update-timestamp` и полное удаление блока `[Software\\CodeWeavers\\CrossOver\\cxoffice]` из файла реестра бутылки (`system.reg`), а также ключа `InstallDate`.

-----

## ⚙️ Настройка

### 1\. Сохранение скрипта

1.  Создайте файл с именем, например, `reset_crossover_full.sh`.
2.  Скопируйте и вставьте в него код, представленный ниже.

### 2\. Редактирование BOTTLE\_NAME

В самом начале скрипта необходимо указать **точное имя** вашей бутылки.

```bash
# ===============================================
# --- НАСТРОЙКИ ---
# ВПИШИТЕ ИМЯ ВАШЕЙ БУТЫЛКИ ЗДЕСЬ
BOTTLE_NAME="Games New" # <--- ИЗМЕНИТЕ ЭТО
# ===============================================
```

### 3\. Установка прав

Сделайте скрипт исполняемым, открыв Терминал и выполнив команду:

```bash
chmod +x reset_crossover_full.sh
```

-----

## 💻 Использование

### 1\. Ручной запуск (для проверки)

Для ручного сброса просто запустите скрипт в Терминале:

```bash
sudo ./reset_crossover_full.sh
```

### 2\. Автоматический запуск (рекомендуется)

Для автоматического сброса меток времени (например, ежедневно перед первым запуском CrossOver) используйте **LaunchAgent** или **Cron**. **LaunchAgent** — предпочтительный способ для macOS, так как он имеет лучшие разрешения.

#### Через Cron (Простой способ)

1.  Откройте редактор Cron:

    ```bash
    crontab -e
    ```

2.  Добавьте следующую строку, указав полный путь к вашему скрипту (`/Users/ВАШЕ_ИМЯ/путь/reset_crossover_full.sh`). Пример запуска **ежедневно в 09:00**:

    ```cron
    0 9 * * * /Users/ВАШЕ_ИМЯ/путь/reset_crossover_full.sh >> /tmp/crossover_reset.log 2>&1
    ```

3.  Сохраните и закройте редактор.

#### ⚠️ Важное примечание о безопасности macOS

На современных версиях macOS (Ventura, Sonoma и т.д.) для успешного изменения файлов в папке `~/Library/Preferences` и завершения процессов (`pkill`) вам может потребоваться предоставить **Full Disk Access (Полный доступ к диску)** в **Системных настройках** для:

  * **Терминала** (или iTerm).
  * Процесса **`/usr/sbin/cron`** (если вы используете Cron).

-----
